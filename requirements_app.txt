# Requirements pour Actor Face Swap Studio
# Application personnalisée basée sur FaceFusion

# IMPORTANT: Utilisez les requirements de FaceFusion directement
# Ce fichier liste les dépendances minimales pour l'interface

# Interface utilisateur
gradio>=4.44.0
gradio-rangeslider>=0.0.8

# Core dependencies (versions compatibles actuelles - Décembre 2025)
numpy>=1.24.0,<2.0.0
onnx>=1.15.0
onnxruntime>=1.16.0
opencv-python>=4.8.0
psutil>=5.9.0
tqdm>=4.65.0
scipy>=1.11.0,<1.15.0

# GPU support (optionnel - décommentez si vous avez une GPU NVIDIA)
# Attention: décommentez ET commentez onnxruntime ci-dessus
# onnxruntime-gpu>=1.16.0

# Utilitaires
pillow>=10.0.0
requests>=2.31.0

# Note: Si vous rencontrez des erreurs de compatibilité,
# installez plutôt directement depuis FaceFusion:
# cd facefusion && pip install -r requirements.txt
